apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: vishaldorage/kubernetes:66
      ports:
        - containerPort: 8080
