apiVersion: v1
kind: Pod
metadata:
  name: javaapp
  labels:
    app: javaapp
spec:
  containers:
    - name: javaapp
      image: vishaldorage/kubernetes:latest
      ports:
        - containerPort: 8080
